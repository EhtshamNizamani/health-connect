import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0057FF);     // Blue
  static const Color text = Color(0xFF1C1C1E);        // Dark text
  static const Color black = Color(0xFF000000);        // Black
  static const Color grey = Color(0xFF8E8E93);        // iOS-style grey
  static const Color border = Color(0xFFE5E5EA);      // Light grey border
  static const Color background = Color(0xFFF2F2F7);  // Soft white background
  static const Color white = Color(0xFFFFFFFF);       // Pure white
  static const Color error = Color(0xFFFF3B30);       // Red (iOS error)
  static const Color disable = Color(0xFFD1D1D6); 
}
